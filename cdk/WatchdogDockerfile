FROM ubuntu:20.04

ENV HEALTHCHECK_PORT=80

RUN apt-get update && apt-get -y upgrade

RUN apt-get -y install python3 && mkdir healthcheck

ENTRYPOINT echo "HI" && python3 -u -m http.server ${HEALTHCHECK_PORT} --directory healthcheck 2>&1
